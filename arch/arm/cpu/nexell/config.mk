
# =========================================================================
#	u-boot.lds path
# =========================================================================
SOCDIR=arch/$(ARCH)/cpu$(if $(CPU),/$(CPU),)$(if $(SOC),/$(SOC),)
LDPPFLAGS += -DSOCDIR=$(SOCDIR)
